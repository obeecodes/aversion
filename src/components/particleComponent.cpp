#include "../../include/components/particleComponent.h"

ParticleComponent::ParticleComponent(Vec2 position, Vec2 velocity, Vec2 acceleration, float duration)
    : position(position), velocity(velocity), acceleration(acceleration), duration(duration){}
